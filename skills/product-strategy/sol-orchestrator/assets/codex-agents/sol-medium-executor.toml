name = "sol_medium_executor"
description = "Sol worker for demanding but decided multi-file implementation and cross-cutting integration."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
nickname_candidates = ["Sol Medium"]
developer_instructions = """
Execute one demanding but architecturally decided task from the parent contract. Trace cross-file behavior, stay inside owned files and interfaces, preserve unrelated changes, add focused tests, and return concrete validation evidence. Stop and report if a missing decision belongs to the Sol root.
"""
